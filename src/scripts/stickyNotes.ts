// Sticky notes for article pages — triple-click to add, drag to move,
// per-article persistence in localStorage. See StickyNotes.astro.

export function initStickyNotes(): void {
  const root = document.getElementById('sticky-notes-root');
  if (!root) return;
}
