---
import CVSection from './CVSection.astro';
import CVTimelineEntry from './CVTimelineEntry.astro';
import type { ExperienceItem } from '../../types/cv';

interface Props {
  items: ExperienceItem[];
}
const { items } = Astro.props;
---

<CVSection title="Experience">
  <div class="space-y-6">
    {items.map((item) => (
      <CVTimelineEntry
        title={item.title}
        sub_title={item.sub_title}
        years={item.years}
        details={item.details?.en}
      >
        {item.achievements && item.achievements.length > 0 && (
          <ul class="space-y-1 mt-1">
            {item.achievements.map((a) => (
              <li class="flex items-start gap-2 text-[13px] text-gray-700 leading-snug">
                <span class="mt-[5px] shrink-0 w-1.5 h-1.5 rounded-full bg-blue-400"></span>
                <span>{a.en}</span>
              </li>
            ))}
          </ul>
        )}
      </CVTimelineEntry>
    ))}
  </div>
</CVSection>
