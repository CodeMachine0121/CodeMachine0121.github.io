---
interface Props {
  title: string;
}
const { title } = Astro.props;
---

<section class="cv-section cv-animate">
  <div class="cv-section__header">
    <span class="cv-section__label">{title}</span>
    <div class="cv-section__rule"></div>
  </div>
  <slot />
</section>
